# Node / client
node_modules/
client/dist/
client/coverage/
*.tsbuildinfo

# Python / server
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
build/

# Dev databases (SQLite artifacts must never be deployed)
*.db
*.sqlite
*.sqlite3

# Generated benchmark reports - regenerate with `make bench`
bench/out/

# Environment / secrets
.env
.env.*
!.env.example
tls/

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db

# graphify knowledge-graph artifacts (machine-local: absolute interpreter
# paths, extraction cache; regenerate with /graphify)
graphify-out/

# Internal working notes, kept out of the public repo: the build guide, the
# ADR / compliance / review set, and local Claude Code tooling config. Rooted
# with a leading slash so only the top-level copies are ignored.
/docs
/CLAUDE.md
/.claude/
/Journal Article/Extra Notes
